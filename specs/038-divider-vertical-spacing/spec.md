# Divider 垂直间距对齐

## 背景与目标

垂直分割线默认左右外边距各 8dp，实际运行时与相邻文案过紧。本次对齐为左右各 16dp。

## 行为契约

- 垂直分割线未配置 Theme margin 时，使用 `spacer16` 作为左右外边距。
- 显式 `TDividerThemeData.margin` 仍拥有更高优先级。
- 水平分割线行为保持不变。

## 兼容性

默认间距发生变化，属于可见默认行为调整。

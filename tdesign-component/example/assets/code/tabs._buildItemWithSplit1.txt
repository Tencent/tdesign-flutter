Widget _buildItemWithSplit1(BuildContext context) {
    return const DefaultTabController(
      length: 2,
      child: TTabsBar(
        tabs: [
          TTab(text: '选项'),
          TTab(text: '选项'),
        ],
      ),
    );
  }

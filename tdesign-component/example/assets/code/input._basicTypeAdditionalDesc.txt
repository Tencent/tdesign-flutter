
  Widget _basicTypeAdditionalDesc(BuildContext context) {
    return TInput(
      type: TInputType.normal,
      leftLabel: '标签文字',
      controller: controller[4],
      hintText: '请输入文字',
      additionInfo: '辅助说明',
      onChanged: (text) {
        setState(() {});
      },
      onClearTap: () {
        controller[4].clear();
        setState(() {});
      },
    );
  }
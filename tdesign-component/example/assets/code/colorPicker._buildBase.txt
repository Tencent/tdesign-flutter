Widget _buildBase(BuildContext context) => TColorPicker(
        value: baseValue,
        onChanged: (result) {
          final (value, _) = result;
          setState(() => baseValue = value);
        },
      );

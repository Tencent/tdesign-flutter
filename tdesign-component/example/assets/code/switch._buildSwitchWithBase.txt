
  Widget _buildSwitchWithBase(BuildContext context) {
    return const TDCell(
      title: '基础开关',
      noteWidget: TDSwitch(),
    );
  }ged: (value) {
          setState(() {
            _formDisableState = value;
          });
          return false;
        },
      ),
      title: '禁用态',
    );
  }
Widget _buildDefaultSearchBar(BuildContext context) {
    return TSearchBar(
      hintText: '搜索预设文案',
      onChanged: (String text) {
        setState(() {
          inputText = text;
        });
      },
    );
  }

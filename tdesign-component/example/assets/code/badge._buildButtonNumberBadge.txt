Widget _buildButtonNumberBadge(BuildContext context) {
    return TBadge(
      count: num,
      child: const TButton(
        child: Text('按钮'),
        size: TButtonSize.large,
      ),
    );
  }

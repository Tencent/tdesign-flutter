Widget _buildBrandFooter(BuildContext context) {
  const logo = TImage(
    src: 'assets/img/t_brand.png',
    width: 104,
    height: 24,
    fit: BoxFit.contain,
  );
  return const Column(
    children: [
      TFooter(logo: logo),
      SizedBox(height: 24),
      TFooter(logo: logo),
    ],
  );
}
